# AGENTS.md — 项目说明（AI 助手参考）

## 项目背景

本项目是一个 **Java 零基础学习项目**，学习者从零开始系统学习 Java 语言。

## 学习者背景

- 学习者有 **PHP 开发经验**，熟悉 PHP 语法与开发思维。
- 在解释 Java 概念时，**应适当与 PHP 进行对比**，帮助学习者通过已有知识快速理解 Java 的差异。

## AI 助手行为准则

1. **面向零基础**：解释时语言简洁易懂，避免过多专业术语，必要时举简单例子。
2. **PHP 对比**：涉及语法、类型、面向对象等概念时，主动与 PHP 做对比说明。
3. **中文回答**：默认使用中文进行解释和沟通。
4. **循序渐进**：按照基础 → 进阶的顺序引导学习，不要跳跃式讲解。

## PHP vs Java 常见对比参考

| 概念 | PHP | Java |
|------|-----|------|
| 变量定义 | `$a = 10;`（弱类型） | `int a = 10;`（强类型，必须指定类型） |
| 小数默认类型 | float | double |
| 字符串 | `"hello"` 或 `'hello'` | `"hello"`（单引号只用于 char） |
| 布尔值 | `true` / `false` | `true` / `false` |
| 打印输出 | `echo` / `var_dump()` | `System.out.println()` |
| 类定义 | `class Foo {}` | `public class Foo {}`（需声明访问修饰符） |
| 入口 | 直接执行 PHP 文件 | 必须有 `main` 方法作为程序入口 |
| 类型转换 | 隐式自动转换（弱类型） | 必须显式转换（强类型） |
